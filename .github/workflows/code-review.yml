name: Simple Bot Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

# 権限の設定を追加
permissions:
  pull-requests: write
  issues: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm install @octokit/rest @actions/github typescript @types/node

      - name: Compile TypeScript
        run: |
          npx tsc .github/scripts/review-comment.ts --esModuleInterop true

      - name: Post review comment
        uses: actions/github-script@v7
        with:
          script: |
            const { postReviewComment } = require('./.github/scripts/review-comment.js');
            await postReviewComment({ github, context });
